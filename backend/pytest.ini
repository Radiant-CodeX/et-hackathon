[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

markers =
    agent: Agent-related tests
    schema: Schema validation tests
    integration: Integration tests
    rag: RAG retrieval tests
    hallucination: Hallucination control tests
